version: 2.1

orbs:
    cypress: cypress-io/cypress@1

workflows:
    "Cypress Workflow":
        jobs:
          - cypress/install
          - cypress/run:
                requires:
                    - cypress/install
                record: true # record results on Cypress Dashboard